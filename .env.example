# Server
PORT=3000
NODE_ENV=production

# Admin panel password — change this before deploying!
ADMIN_PASSWORD=changeme123

# Session secret — generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change_this_to_a_random_secret

# Your site URL (used for OG tags and share links)
SITE_URL=https://yourdomain.com

# Nedarim Plus payment integration
# Get these from your Nedarim Plus account at matara.pro
MOSAD_ID=
API_VALID=

# Nedarim Plus webhook IP (default is their production IP — don't change unless they tell you)
NEDARIM_WEBHOOK_IP=18.194.219.73
